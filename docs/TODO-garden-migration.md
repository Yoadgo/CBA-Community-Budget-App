# הגינון — מה בייצור, ומה נשאר   (עודכן 16.9.2026)

## ✅ בייצור ועובד

| מה | מצב |
|---|---|
| Apps Script | **גרסה 138** (מזהה הפריסה לא השתנה) |
| כללי האבטחה | **פורסמו** — 45,160 תווים, כולל כללי כתיבה |
| לקוח | `?v=20260916n` |
| מונים | נזרעו: דיווח 7, משימה 48 |
| "הדיווחים שלי" | **חי, 30ms** (היה 30,002ms — timeout) |
| 24 מקרי כתיבה | נבדקו מול הכללים החיים, כולם כצפוי |

**דגלים דלוקים:** gardenPlan · services · budgetYear · budgetTx ·
pulse · boot · homeCounts · tour · clubResv · **gardenReports**

---

## ⛔ חסום — צריך דיפלוי אחד

**קומיט `916910e` (תיקון סינון שנת התקציב) נמצא ב-GitHub אבל
לא נפרס.** ניסיתי שלוש פעמים ברצף והשמירה בעורך לא נתפסה — אותו
מסלול בדיוק עבד פעמיים קודם באותה שיחה, כלומר זה הבהוב ולא חסימה.

### מה צריך לעשות
1. עורך Apps Script, מחובר כ-`gizbar30` → להדביק את `Code.gs`
   מהקומיט `11a3b8f` → **לשמור ולוודא אחרי רענון** שהאורך 708,114.
2. **לפריסה ← ניהול הפריסות ← עיפרון ← גרסה חדשה ← לפריסה.**
   ⚠️ לא "פריסה חדשה" מהתפריט העליון.
3. להריץ `?action=gardenDataSync` ממסך "מצב המערכת".
4. 🔴 **להשוות ספירות:** `gardenTasks` ב-Firestore חייב להחזיר
   **41** — כמו Apps Script. אם פחות, **לא להדליק**.
5. רק אז להדליק `gardenTasksFromFirestore`.

---

## 🟡 כבוי בכוונה — החלטה שלך

**`gardenWriteToFirestore`** — הדפדפן כותב דיווח ישירות ל-Firestore.
הקוד והכללים נבדקו, אבל **לא הרצתי אותו מקצה לקצה בייצור**, כי כדי
לאמת צריך להגיש דיווח אמיתי — וזה יוצר משימה בתור של הצוות ושולח
מיילים לאנשים אמיתיים.

**כשתרצה:** להדליק ממסך "מצב המערכת", להגיש דיווח אחד, ולוודא
שהוא מופיע מיד ושהמייל יצא. אם משהו נשבר — לכבות, והמסלול הישן
חוזר מיד.

---

## ⏭ לא נעשה — וזה הגבול האמיתי של ההיפוך

**עשר פעולות הניהול של המשימות עדיין כותבות לגיליון** (ומסנכרנות
מיד, ולכן אין פער). הן מחזיקות לוגיקה עסקית שכלל אבטחה אינו יודע
לאכוף — "חובה לכתוב מה נעשה", "ביטול רק בתוך חלון הערעור".

**זה לא חוב טכני שנשכח — זו החלטה שלך** (16.9): מה שרץ בדפדפן
אפשר לעקוף, ולוגיקה שקובעת מה מותר לא שייכת שם.

---

## 🧹 ניקיון

`gardenLog/RTL1` — מסמך בדיקה שנוצר באימות הכללים. מצביע על משימה
שלא קיימת, אינו מוצג בשום מסך. לא מחקתי — מחיקת נתונים אינה פעולה
שאני עושה לבד.
